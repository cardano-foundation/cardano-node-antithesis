# Resurrection record — epic owner, cna#205

Updated 2026-09-07T12:2xZ. Authored by this seat (`%432`), not ghost-written.
Derived from `STATUS.md` (append-only) plus live GitHub and tmux reads.

## Seat and parentage

| field | value |
|---|---|
| role | EPIC OWNER, cna#205 |
| pane | `%432`, window `amaru:2 cna-e205-lane`, top-left |
| cli | claude `opus-5[1m]` |
| root | `/tmp/ms-cardano-node-antithesis-2/e-auto` |
| map | `epic-map.md` |
| **parent** | **M2 owner `%694` (codex), window `amaru:1 ms2-amaru-routine`** — restored 2026-09-07 by NOTE-007 |
| repos | `/code/cardano-node-antithesis` (main `6711c4ab8fe02f6418897b73bf42ca41e6efa697`), `/code/amaru-bootstrap` |

**Supersedes the earlier claim that no milestone seat exists above this root.**
It was true when written and is now false: report upward to `%694`, through the
journal.

## Exact replay command

Verified 2026-09-07 from `/proc/2497522/cmdline` (child of pane `%432`'s shell
PID `2497304`), not copied from a prior record:

```sh
cd /code/cardano-node-antithesis
claude --dangerously-skip-permissions --model 'claude-opus-5[1m]' --effort high
```

The argv tokens are exactly `claude`, `--dangerously-skip-permissions`,
`--model`, `claude-opus-5[1m]`, `--effort`, `high`, and the pane's cwd is
`/code/cardano-node-antithesis`.

**The quotes around `claude-opus-5[1m]` are mandatory to replay it, even though
they do not appear in argv** — the shell consumed them at launch. Unquoted,
`[1m]` is a bracket glob and is at the mercy of the launching directory and the
shell's `nullglob`/`failglob` settings. Dropping the `[1m]` suffix entirely pins
a different, smaller-context session, which is the wrong seat for a long-lived
orchestrator. Replay this line as written.

On resurrection, after launching: read `STATUS.md` (append-only, it is the
memory), then `epic-map.md`, then this file. Re-announce with a post-cursor
`START` naming the new pane id.

## Stage — ACTIVE

Lane released 2026-09-07T08:31Z. PR#239 merged `6711c4a` (the observation-window
fix). Ticket `t-unchanged-head` in flight.

## Children

- **`t-unchanged-head`** — ticket owner `%430` (claude `opus-5[1m]`, `context=REUSED`),
  root `t-unchanged-head/`, brief sha256 `529d001a…`, base `6711c4a`, `START`
  08:41:19Z. Branch `fix/unchanged-head-after-success`, worktree
  `/code/cna-unchanged-head`.
  Its commit owner is `%675` (glm-5.3-flash) — **a grandchild: never contact it.**
- `t-check-observation` — COMPLETE, PR#239 merged, root archived to
  `.archived/t-check-observation`.

## The lane-operations role — retired, deliberately

`seat-308-handoff.md` (sha256 `676a1d91…`) describes a lane-operations successor
seat. `%308` is gone; `%694` is the M2 desk and correctly refuses the subordinate
role. **I retired the role rather than reseating it:** its only standing duty was
the read-only scheduled-run verification, which this seat already owns and had
already claimed in writing. Two owners over one duty is the Q-EPIC-006 error in
miniature.

**Binding correction:** that packet told its successor to append to *this*
journal. A process writes only its own STATUS. Any future lane seat gets its own
runtime root; `e-auto/STATUS.md` is written by `%432` alone. Existing foreign
entries are preserved untouched as evidence.

## Exact next actions

1. **Read the 2026-09-08T04:17Z scheduled run — the first carrying #239. This
   seat's duty. Never fire or re-run it.**
2. Supervise `t-unchanged-head/STATUS.md`; verify artifacts, not events. Accept
   or return findings; merge is mine.
3. Then the queue: `F-OBS-01`, `F-OBS-05`, `F-OBS-02`, `F-OBS-03`, ab#75 (head of
   the epic order, needs a fresh T.O. — worktree, root and pane all gone), ab#79,
   ab#110, cna#212, cna#232/PR#233, #208, #207, #206.

## Standing fences

- cna is third-party: **no issue, no PR comment, no new issue.** Draft PR is the
  end state; human-addressed items go to `handoffs/`.
- **No nightly re-runs to fish for green.** Read the logs you have.
- Never run `nixos-rebuild`. No Rust in this repo's user code — Rust-shaped errors
  come from Antithesis-shipped binaries.
- No sideways contact with ticket-owner or worker panes; immediate child only.
- A-EPIC-003: the upstream `pragma-org/amaru` era-history fix is drafted, never sent.

## Honest gaps

- The epic artifact (`daily-amaru.yaml`) has **never been released**; no pipeline
  publishes one.
- **Mode (b)** — the true silent exit emitting nothing — has not recurred since
  08-31, is unaddressed, and is **open and unproven in both directions**. It is not
  what the 09-04→09-07 reds were.
- No claim exists, from this seat or its children, that #239 makes the nightly
  green. Only the 09-08 run can say.
- Standing expectation, three instances so far: **repairing the thing arms the next
  trap.** After every fix, ask what it just made reachable.
