# Plan
Ceiling: 70 lines / 4 KiB.

One bounded repair slice follows a reproduced actual-path failure. Preserve the
external bootstrap resolver/anchor as the rule authority; reuse the daily stage
contracts. The owner chooses internal implementation within the invariant fence.

P-0: Admit only the six exact planning paths to the historical test allowlist under
A-001. Preserve its baseline, predicate and out-of-fence negative control. This
owner-authored test-only preparation may precede draft PR creation; verify local CI.
P-1: Freeze the compact mandate and runtime gate; retain the baseline RED receipt.
Create the scoped draft PR before production repair.
P-2: Owner ships executable regression/negative controls, demonstrates RED, then
implements the smallest repair and verifies the focused and full ticket gates.
P-3: Fresh blind inspectors review the exact candidate and preparation diff, with
one consolidated findings batch and at most one repair/delta-audit submission.
P-4: Stamp accepted tasks, verify the final tree, full gate and remote checks at the
exact pushed head, and request merge. No merge is authorized here.

Live boundary: actual transport with private local clone and real Nix/resolver
reads; GitHub writes and git push are mechanically denied. A non-secret identity
placeholder replaces credentials. Downstream tests use explicit local adapters;
production consumer preparation and Antithesis dispatch remain prohibited.

Canonical CI is nix develop --quiet -c just ci. Runtime gate covers actual
already-pinned selection plus that full recipe. Existing-source checks/digest and
meaningful failure controls are permanent tests, independently inspected for
reach, distinct values and non-vacuity. Frozen gates stay untracked and ignored.
